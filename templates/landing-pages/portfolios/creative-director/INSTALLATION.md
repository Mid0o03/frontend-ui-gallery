# Installation Guide

Follow these steps to set up the "Creative Director" template on your local machine.

## 📋 Prerequisites
- **Node.js**: Version 18.x or higher.
- **npm**: Version 9.x or higher.

## ⚙️ Step-by-Step Setup
1. **Clone or Download**: Extract the zip file to your working directory.
2. **Install Dependencies**:
   ```bash
   npm install
   ```
3. **Run Development Server**:
   ```bash
   npm run dev
   ```
   The site will be available at `http://localhost:5173`.

## 🛠 Troubleshooting
- **Node version error**: Ensure you are using a compatible Node version (`node -v`).
- **Missing dependencies**: If you see "module not found" errors, run `npm install` again.
- **Port already in use**: Vite will automatically pick another port, check the terminal output.

---

# Guide d'Installation

Suivez ces étapes pour installer le template "Creative Director" sur votre machine locale.

## 📋 Prérequis
- **Node.js** : Version 18.x ou supérieure.
- **npm** : Version 9.x ou supérieure.

## ⚙️ Configuration Étape par Étape
1. **Cloner ou Télécharger** : Extrayez le fichier zip dans votre dossier de travail.
2. **Installer les Dépendances** :
   ```bash
   npm install
   ```
3. **Lancer le Serveur de Développement** :
   ```bash
   npm run dev
   ```
   Le site sera accessible à `http://localhost:5173`.

## 🛠 Dépannage
- **Erreur de version Node** : Vérifiez votre version avec `node -v`.
- **Dépendances manquantes** : Si vous avez des erreurs "module not found", relancez `npm install`.
