import React from 'react';
import { Hero } from '../../components/Hero';
import { Quote } from '../../components/Quote';

const Home: React.FC = () => {
    return (
        <>
            <Hero />
            <Quote />
        </>
    );
};

export default Home;
